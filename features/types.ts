const today = new Date();
today.getMonth();

const person = {
    age: 20
};

// class example

class Color {}
const red = new Color();

/**
 * every value associated with the property used in the code must contains the types.
 */